[env:atoms3]
platform = espressif32
board = m5stack-atoms3
framework = arduino
monitor_speed = 115200
; Route Arduino `Serial` to native USB CDC so the host can drive MIDI passthrough.
build_flags = -DARDUINO_USB_CDC_ON_BOOT=1
